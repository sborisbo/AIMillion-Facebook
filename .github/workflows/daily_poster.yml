name: Auto Post to Facebook

on:
  schedule:
    - cron: '0 9 * * *' # Время указывается в формате UTC. 12:00 по Израилю (UTC+3) — это 09:00 UTC.
  workflow_dispatch: # Оставляет возможность запустить вручную при необходимости

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install feedparser requests google-genai

      - name: Run python auto_news_poster.py
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
          FB_PAGE_ACCESS_TOKEN: ${{ secrets.FB_PAGE_ACCESS_TOKEN }}
        run: python auto_news_poster.py
